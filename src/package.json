{
  "private": true,
  "name": "akshar-prospera-proxy",
  "version": "0.0.0",
  "scripts": {
    "install": "npm --prefix .. install",
    "build": "npm --prefix .. run build",
    "dev": "npm --prefix .. run dev",
    "preview": "npm --prefix .. run preview"
  }
}
